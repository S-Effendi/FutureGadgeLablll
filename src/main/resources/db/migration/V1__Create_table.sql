CREATE TABLE Ticket (

ticketId int generated BY DEFAULT AS identity PRIMARY KEY,
ticketAvailable BOOLEAN DEFAULT FALSE,
entryTime timestamp DEFAULT current_timestamp NOT NULL,
exitTime timestamp,
fee float

);
